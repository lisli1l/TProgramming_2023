#include <algorithm>

class Solution
{
  public:
    static int maxSum(TreeNode* root)
    {
      if (root == NULL)
        return 0;
      else
        return root->value + (std::max(maxSum(root->left), maxSum(root->right)));
    }
};